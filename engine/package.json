{
  "name": "@plantry/engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./library": "./src/data/library.ts",
    "./history": "./src/data/history.ts"
  },
  "scripts": {
    "bake": "tsc -b && node dist/scripts/bake.js",
    "prebuild": "npm run bake",
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@vitest/coverage-v8": "^2.1.1",
    "vitest": "^2.1.1"
  }
}
